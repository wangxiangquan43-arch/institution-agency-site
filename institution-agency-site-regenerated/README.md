# institution-agency-site

教育机构直播陪跑官网，基于 Vite + React。

## 本地运行

```bash
npm install
npm run dev
```

## 构建

```bash
npm run build
```

## 部署到 Vercel

1. 把本项目上传到 GitHub 仓库
2. 在 Vercel 导入该仓库
3. 保持默认构建命令 `npm run build`
4. 输出目录为 `dist`

注意：本项目 **不包含 package-lock.json**，避免锁文件导致远程安装依赖卡住。
